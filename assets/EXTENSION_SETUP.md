# Extension Runtime
The desktop app ships an unpacked extension in `/extension`.
If Chrome blocks auto-loading, open `chrome://extensions`, enable Developer mode, and click Load unpacked -> choose `extension/`.
